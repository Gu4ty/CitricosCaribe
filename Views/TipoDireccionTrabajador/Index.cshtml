@model IEnumerable<CitricosCaribe.Models.TipoDireccionTrabajador>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trabajador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Trabajador.CI)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrabajadorID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrabajadorID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrabajadorID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
